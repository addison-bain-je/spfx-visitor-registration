import * as React from 'react';
import { IconButton } from '@material-ui/core';
import DeleteIcon from '@material-ui/icons/Remove';
/*
import * as Highcharts from 'highcharts';
import HighchartsReact from 'highcharts-react-official';
const options: Highcharts.Options = {
  title: {
    text: 'count of request by application'
  },
  xAxis: {
    type: 'category'
  },
  yAxis: {
    title: {
      text: 'count of request'
    }

  },
  legend: {
    enabled: true
  },
  series: [{
    //name: "application",
    type: 'column',
    data: [{ name: "A", y: 1 }, { name: "B", y: 7 }, { name: "C", y: 10 },]
  }]
};


const pieoptions: Highcharts.Options = {
  title: {
    text: 'percentage of request by BU Group'
  },
  xAxis: {
    type: 'category'
  },
  yAxis: {
    title: {
      text: 'percentage of request by BU Group'
    }

  },
  legend: {
    enabled: true
  },
  plotOptions: {
    series: {
      //pointPadding: 0.4,
      borderWidth: 0,
      dataLabels: {
        enabled: true,
        //format: '{point.y:.1f}%'
        //format:'{point.name}:{point.y:.1f}%'
        format:'{point.name}'
      }
    }
  },

  tooltip: {
    headerFormat: '<span style="font-size:11px">{point.name}</span><br>',
    pointFormat: '<span style="color:{point.color}">{point.name}</span>: <b>{point.y:.2f}%</b> of total<br/>'
  },
  series: [{
    name: "percentage",
    type: 'pie',
    data: [{ name: "APG", y: 20 }, { name: "IPG", y: 20 }, { name: "Other", y: 60 },]
  }]
};
*/

function Dummy(props) {
  //const { children, ...other } = props;
  
  return (
    <>
     


    </>
  );

}
export default Dummy;
